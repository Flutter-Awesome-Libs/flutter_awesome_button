name: awesome_ui_button
description: A quick way to make a good looking button with a little flair, just import and use.
version: 1.0.2
homepage: https://github.com/Flutter-Awesome-Libs/flutter_awesome_button

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
